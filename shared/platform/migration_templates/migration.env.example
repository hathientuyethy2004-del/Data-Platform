# Migration flags - example
# false/true, 0/1, yes/no đều được parser hỗ trợ

# Route write/processing paths
USE_OSS_INGESTION=false
USE_OSS_QUALITY=false
USE_OSS_SERVING=false

# Route read path (serving query)
READ_FROM_OSS_SERVING=false

# Dual-run mode (chạy legacy + oss song song)
DUAL_RUN_ENABLED=true
DUAL_RUN_COMPARE_ENABLED=true
